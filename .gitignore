.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Internal build-tracking / superstack workspace state
.superstack/

# Local-only Claude bridge to the stewfinance brain vault — never commit
CLAUDE.local.md

# Local keypairs — never commit
*-keypair.json
co-signer-keypair.json
wallet-*.json
test-wallet.json

# Env files
.env
.env.local
.env.*.local
web/.env.local

# Devnet faucet mint-authority secret (thin demo) — never commit
.faucet-authority.json
# Local funnel-events sink (thin demo, used when Supabase is not configured)
web/.demo-events.local.jsonl

# surfpool local workspace + logs
.surfpool/
.superstack/surf/
